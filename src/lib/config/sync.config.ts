/**
 * TODO: Re-enable cloud sync when AUTH_DISABLED=false and Clerk auth is restored.
 * Set CLOUD_SYNC_DISABLED=false after backend persistence (DATABASE_URL) is configured.
 */
export const CLOUD_SYNC_DISABLED = true;
