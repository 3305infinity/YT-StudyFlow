# Chrome extension build
# Set this to your deployed Express backend before building the extension package.
VITE_API_BASE_URL=https://YOUR_BACKEND_HOST

# Auth web URL is unused while authentication is disabled.
# TODO: Re-enable when Clerk sign-in returns.
VITE_AUTH_WEB_URL=
